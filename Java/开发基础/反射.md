
# 静态 动态语言

![[Pasted image 20251117174316.png]]

## 动态语言
![[Pasted image 20251117174532.png]]
js中
原本变量x是一个字符串类型，但是在网站运行过程中，可以改变x的类型
从字符串变成了一个整数类型
这既是动态语言



# 什么是反射（Reflection）
![[Pasted image 20251117174750.png]]


# class类的常用方法
![[Pasted image 20251117200858.png]]



![[Pasted image 20251117201116.png]]
具体如下
```java
package test;  
//测试class类的创建方式有哪些  
public class test03 {  
    public static void main(String[] args) throws ClassNotFoundException{  
        Person person=new Student();  
        System.out.println("这个人是："+person.name);  
  
  
        //方式一：通过对象获得  
        Class c1=person.getClass();  
        System.out.println(c1.hashCode());  
  
  
        //方式2：forname获得  
        Class c2=Class.forName("test.Student");  
        System.out.println(c2.hashCode());  
  
  
        //方式三；通过类名.class  
        Class c3 = Student.class;//写Student.class;按alt+ctrl+v  
        System.out.println(c3.hashCode());  
  
        //方式四：基本内置类型的包装类都有一个Type属性  
        Class c4 = Integer.TYPE;  
        System.out.println(c4);  
  
        //获得父类类型  
        Class c5 = c1.getSuperclass();  
        System.out.println(c5);  
  
    }  
}  
class Person{  
    public String name;  
  
    public Person() {  
    }  
  
    public Person(String name) {  
        this.name = name;  
    }  
  
    @Override  
    public String toString() {  
        return "Person{" +  
                "name='" + name + '\'' +  
                '}';  
    }  
}  
  
class Student extends Person{  
    public Student(){  
        this.name="学生";  
    }  
}  
class Teacher extends Person{  
    public Teacher(){  
        this.name="老师";  
    }  
}
```
输出如下
![[Pasted image 20251117205602.png]]
```java
        //方式四：基本内置类型的包装类都有一个Type属性  
        Class c4 = Integer.TYPE;  
        System.out.println(c4);  
```
这段代码的作用是：**获取 Java 基本类型** `int` **的** `Class` **对象**，并打印出来。

### 🔍 逐行解释

java

```
Class c4 = Integer.TYPE;
System.out.println(c4);
```

#### ✅ `Integer.TYPE`

- `Integer` 是 Java 中的 **包装类**，用于封装基本类型 `int`。
    
- `Integer.TYPE` 是一个 **静态字段**，表示 `int.class`，也就是 `int` 类型的 `Class` 对象。
    
- 所有基本类型的包装类（如 `Integer`, `Double`, `Boolean` 等）都有一个 `.TYPE` 字段，指向对应的基本类型。
    

#### ✅ `Class c4 = Integer.TYPE;`

- 这行代码将 `int` 类型的 `Class` 对象赋值给变量 `c4`。
    
- `c4` 实际上是 `int.class`，也可以写成：
    
    java
    
    ```
    Class c4 = int.class;
    ```
    

#### ✅ `System.out.println(c4);`

- 打印 `c4` 的内容，会输出：
    
    代码
    
    ```
    int
    ```
    
    表示这是 `int` 类型的 `Class` 对象。
    

### 📦 类似的包装类 `.TYPE` 对照表

|包装类|基本类型|`.TYPE` 等价于|
|---|---|---|
|`Integer`|`int`|`int.class`|
|`Double`|`double`|`double.class`|
|`Boolean`|`boolean`|`boolean.class`|
|`Character`|`char`|`char.class`|
|`Long`|`long`|`long.class`|
|`Float`|`float`|`float.class`|
|`Short`|`short`|`short.class`|
|`Byte`|`byte`|`byte.class`|


# 哪些类型可以有class对象
![[Pasted image 20251117215507.png]]


```java
package test;  
  
import java.lang.annotation.ElementType;  
  
//所有类型的class  
public class test04 {  
    public static void main(String[] args) {  
  
  
        Class c1 = Object.class;//类  
        Class c2 = Comparable.class;//接口  
        Class c3 = String[].class;//一维数组  
        Class c4 = int[][].class;//二维数组  
        Class c5 = Override.class;//注解  
        Class c6 = ElementType.class;//枚举  
        Class c7 = Integer.class;//基本数据类型  
        Class c8 = void.class;//void  
        Class c9 = Class.class;//Class  
  
        System.out.println(c1);  
        System.out.println(c2);  
        System.out.println(c3);  
        System.out.println(c4);  
        System.out.println(c5);  
        System.out.println(c6);  
        System.out.println(c7);  
        System.out.println(c8);  
        System.out.println(c9);  
        //元素类型和维度一样，就是一个class  
        int[]a=new int[10];  
        int[]b=new int[100];  
        System.out.println(a.getClass().hashCode());  
        System.out.println(b.getClass().hashCode());  
    }  
  
  
}
```


```输出
class java.lang.Object
interface java.lang.Comparable
class [Ljava.lang.String;
class [[I
interface java.lang.Override
class java.lang.annotation.ElementType
class java.lang.Integer
void
class java.lang.Class
189568618
189568618
```
# java中class内存分析
![[Pasted image 20251117224912.png]]
加载方法区
先再堆里生成一个class对象
链接到栈里m=0
再进行初始化
把静态代码块中的值按顺序赋给m


![[Pasted image 20251118101539.png]]

![[Pasted image 20251118101525.png]]
![[Pasted image 20251118101722.png]]


# 分析类的初始化
![[Pasted image 20251118101949.png]]
```java
package test;  
  
public class test06 {  
    static {  
        System.out.println("main被加载");  
    }  
  
    public static void main(String[] args) throws ClassNotFoundException {  
        //1.1主动引用子类  
        Son son=new Son();  //都会初始化
        //1.2反射也会产生主动引用  
        Class.forName("test.Son");  //都会初始化
        /*  
和 new Son() 的区别  
new Son()                   加载类（如果没加载过），执行静态初始化块，然后执行构造函数，创建对象实例  
Class.forName("test.Son")   加载类并执行静态初始化块，但不会创建对象，只返回 Class 对象  
  
         */        
         
         
         
         
         //2.不会产生类的饮用方法  
        System.out.println(Son.b);//输出2  
  
        Son[] arryay=new Son[5];//只开辟空间给这空间命名而已，并不会初始化一个类  
  
        System.out.println(Son.M);  
  
    }  
}  
  
  
class Father {  
    static int b = 2;  
  
    static {  
        System.out.println("父类被加载");  
    }  
}  
  
  
  
class Son extends Father{  
    static{  
        System.out.println("子类被加载");  
        m=300;  
    }  
    static int m=100;  
    static final int M=1;  
}
```

# 类加载器
![[Pasted image 20251118104137.png]]
![[Pasted image 20251118104353.png]]
